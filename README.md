# AccessibilityCheck

A Figma plugin prototype for AI-powered accessibility checking with on-demand explanations.

## Structure

- `ui.html`: plugin UI
- `code.ts`: Figma plugin controller
- `backend/server.js`: local backend for GPT calls and logging

## Setup

### Plugin

```bash
npm install
npm run build
cd backend
npm install
npm run dev


## 8. If you want GitHub Desktop instead

You can also:
- open GitHub Desktop
- add the local folder
- publish repository

That is easier if you prefer GUI over terminal.

## 9. Most important warning

Do **not** push:
- API keys
- participant logs
- raw study data

Your `.gitignore` should handle that.
```
